# README

Due to the nature of this software, will probably need 2 containers:

- A Singularity container will probably be needed to run all the computational analyses on the reads
- A Docker container will probably be needed to run the binning app locally (until a web app is released)